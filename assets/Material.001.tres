[gd_resource type="ShaderMaterial" format=3 uid="uid://c1yfewd1rdv81"]

[ext_resource type="Texture2D" uid="uid://debrf63tjsaij" path="res://assets/grass_grass1.png" id="1_fcj5y"]

[sub_resource type="Shader" id="Shader_fcj5y"]
code = "// NOTE: Shader automatically converted from Godot Engine 4.6.1.stable's StandardMaterial3D.

shader_type spatial;
render_mode blend_mix, depth_draw_opaque, cull_disabled, diffuse_burley, specular_schlick_ggx, alpha_to_coverage;

uniform vec4 color_top : source_color = vec4(1.0, 1.0, 1.0, 1.0);
uniform vec4 color_bottom : source_color = vec4(0.0, 0.3, 0.0, 1.0);
uniform sampler2D texture_albedo : source_color, filter_linear_mipmap, repeat_enable;
uniform float alpha_scissor_threshold : hint_range(0.0, 1.0, 0.001);
uniform ivec2 albedo_texture_size;
uniform float point_size : hint_range(0.1, 128.0, 0.1);

uniform float roughness : hint_range(0.0, 1.0);
uniform sampler2D texture_metallic : hint_default_white, filter_linear_mipmap, repeat_enable;
uniform vec4 metallic_texture_channel;
uniform sampler2D texture_roughness : hint_roughness_r, filter_linear_mipmap, repeat_enable;

uniform float specular : hint_range(0.0, 1.0, 0.01);
uniform float metallic : hint_range(0.0, 1.0, 0.01);

uniform vec3 uv1_scale;
uniform vec3 uv1_offset;
uniform vec3 uv2_scale;
uniform vec3 uv2_offset;

void vertex() {
	UV = UV * uv1_scale.xy + uv1_offset.xy;
}

void fragment() {
	vec2 base_uv = UV;

	vec4 albedo_tex = texture(texture_albedo, base_uv);
	vec4 gradient_color = mix(color_top, color_bottom, base_uv.y);
	ALBEDO = gradient_color.rgb * albedo_tex.rgb;

	float metallic_tex = dot(texture(texture_metallic, base_uv), metallic_texture_channel);
	METALLIC = metallic_tex * metallic;
	SPECULAR = specular;

	vec4 roughness_texture_channel = vec4(1.0, 0.0, 0.0, 0.0);
	float roughness_tex = dot(texture(texture_roughness, base_uv), roughness_texture_channel);
	ROUGHNESS = roughness_tex * roughness;
	ALPHA *= gradient_color.a * albedo_tex.a;
	ALPHA_SCISSOR_THRESHOLD = alpha_scissor_threshold;
	ALPHA_ANTIALIASING_EDGE = 0.9;
	ALPHA_TEXTURE_COORDINATE = UV * vec2(albedo_texture_size);
	//ALPHA_HASH_SCALE = 2.0;
	if(!FRONT_FACING) {
		NORMAL = -NORMAL;
	}
}
"

[resource]
resource_name = "Material.001"
render_priority = 0
shader = SubResource("Shader_fcj5y")
shader_parameter/color_top = Color(0.38521668, 0.58, 0.20300001, 1)
shader_parameter/color_bottom = Color(0.1969, 0.44, 0.15400001, 1)
shader_parameter/texture_albedo = ExtResource("1_fcj5y")
shader_parameter/alpha_scissor_threshold = 0.990000047025
shader_parameter/albedo_texture_size = Vector2i(605, 350)
shader_parameter/point_size = 0.0
shader_parameter/roughness = 0.6790000322525
shader_parameter/metallic_texture_channel = Vector4(1, 0, 0, 0)
shader_parameter/specular = 0.30999999307088
shader_parameter/metallic = 0.0
shader_parameter/uv1_scale = Vector3(1, 1, 1)
shader_parameter/uv1_offset = Vector3(0, 0, 0)
shader_parameter/uv2_scale = Vector3(1, 1, 1)
shader_parameter/uv2_offset = Vector3(0, 0, 0)
